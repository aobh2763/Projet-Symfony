<!-- Filter Options -->
{{ form_start(pickTypeForm) }}

<div class="row">
    <div class="col-md-6 mb-3 mb-md-0">
        <div class="dropdown">
            {{ form_widget(pickTypeForm.product_type, {'attr': {'class': 'btn'}}) }}
        </div>
    </div>
</div>

{{ form_end(pickTypeForm, { 'render_rest': false }) }}
